# Changelog — v0.3.3

- Added a dedicated search checkbox for every supported portal.
- Added Select all / Select none controls and a selected-source counter.
- Search is blocked with a clear message when no portal is selected.
- Portal selection is persisted in the embedded UI and restored on the next open.
- Added a regression test that requires the UI checkbox set to exactly match the registered `_SEARCHERS` adapters.
